import { ButtonWarningDirective } from './button-warning.directive';

describe('ButtonWarningDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonWarningDirective();
    expect(directive).toBeTruthy();
  });
});
