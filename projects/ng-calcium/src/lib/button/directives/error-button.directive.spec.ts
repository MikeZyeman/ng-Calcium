import { ErrorButtonDirective } from './error-button.directive';

describe('ErrorButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ErrorButtonDirective();
    expect(directive).toBeTruthy();
  });
});
