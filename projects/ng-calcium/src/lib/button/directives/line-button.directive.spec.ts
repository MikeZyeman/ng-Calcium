import { LineButtonDirective } from './line-button.directive';

describe('LineButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new LineButtonDirective();
    expect(directive).toBeTruthy();
  });
});
